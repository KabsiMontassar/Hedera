{
  "name": "hedera-nft-badges",
  "version": "1.0.0",
  "description": "A server for minting NFT badges on Hedera when users complete tasks or courses",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@hashgraph/sdk": "^2.18.0",
    "express": "^4.18.2",
    "body-parser": "^1.20.1",
    "dotenv": "^16.0.3",
    "mongoose": "^6.8.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
